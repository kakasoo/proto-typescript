export namespace ErrorType {
  export type TO_HAVE_TO_BE_BIGGER_THAN_FROM = '`TO` have to be bigger than `FROM`';
  export type IS_NOT_INT_FORMAT = 'is not `int` format';
  export type CONSTRUCTOR_RECEIVE_NOTHING_1 = 'If Range is given as Generic, the constructor receives nothing.';
}
